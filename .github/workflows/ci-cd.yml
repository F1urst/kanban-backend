name: CI/CD Backend

on:
  push:
    branches: [ main, master ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.NAME }}
          password: ${{ secrets.PASSWORD }}

      - name: Build and tag
        run: |
          docker build -t ${{ secrets.NAME }}/kanban-backend:latest .
          docker tag ${{ secrets.NAME }}/kanban-backend:latest ${{ secrets.NAME }}/kanban-backend:${{ github.sha }}

      - name: Push to Docker Hub
        run: |
          docker push ${{ secrets.NAME }}/kanban-backend:latest
          docker push ${{ secrets.NAME }}/kanban-backend:${{ github.sha }}
