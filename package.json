- name: Create package.json
  run: |
    cat > package.json <<EOF
    {
      "name": "kanban-backend",
      "version": "1.0.0",
      "main": "server.js",
      "dependencies": {
        "express": "^4.18.2"
      }
    }
